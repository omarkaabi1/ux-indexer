"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const assert_1 = __importDefault(require("assert"));
const vitest_1 = require("vitest");
const client_1 = require("./client");
const DEFAULT_SCHEDULE = [10, 100, 500, 2000, 10000, 20000];
(0, vitest_1.describe)('HttpClient retry configuration', () => {
    (0, vitest_1.it)('exposes the default schedule when none was given', () => {
        assert_1.default.deepStrictEqual([...new client_1.HttpClient({ log: null }).retrySchedule], DEFAULT_SCHEDULE);
    });
    (0, vitest_1.it)('exposes a configured schedule', () => {
        let client = new client_1.HttpClient({ log: null, retrySchedule: [1, 2, 3] });
        assert_1.default.deepStrictEqual([...client.retrySchedule], [1, 2, 3]);
    });
    (0, vitest_1.it)('copies the configured schedule instead of aliasing it', () => {
        // The property is exposed readonly, so mutating the array the caller passed in
        // must not retune a live client.
        let schedule = [1, 2, 3];
        let client = new client_1.HttpClient({ log: null, retrySchedule: schedule });
        schedule.push(999);
        assert_1.default.deepStrictEqual([...client.retrySchedule], [1, 2, 3]);
    });
    (0, vitest_1.it)('keeps an empty schedule rather than falling back to the default', () => {
        assert_1.default.deepStrictEqual([...new client_1.HttpClient({ log: null, retrySchedule: [] }).retrySchedule], []);
    });
    (0, vitest_1.it)('exposes the configured retryAttempts, and 0 when unset', () => {
        assert_1.default.strictEqual(new client_1.HttpClient({ log: null, retryAttempts: 4 }).retryAttempts, 4);
        assert_1.default.strictEqual(new client_1.HttpClient({ log: null, retryAttempts: Infinity }).retryAttempts, Infinity);
        assert_1.default.strictEqual(new client_1.HttpClient({ log: null }).retryAttempts, 0);
    });
});
//# sourceMappingURL=client.test.js.map