import { ClientBase } from 'pg';
export declare const db_config: {
    host: string;
    port: number;
    user: string;
    password: string;
    database: string;
};
export declare function withClient(block: (client: ClientBase) => Promise<void>): Promise<void>;
/**
 * The DB_* env vars that select a connection. Tests that exercise
 * `createConnectionOptions()` mutate these, so they save/restore around each case.
 */
export declare const DB_ENV_VARS: string[];
export declare function clearDbEnv(): void;
export declare function snapshotDbEnv(): Record<string, string | undefined>;
export declare function restoreDbEnv(snap: Record<string, string | undefined>): void;
export declare const SCHEMA_ENV_VARS: string[];
/**
 * Registers hooks that clear the schema env vars before each test and restore
 * them after, WITHOUT touching the connection vars (`DB_HOST`/`DB_PORT`/...).
 * Use it in live-DB suites so an ambient `DB_SCHEMA` in the shell/CI can't make
 * the default-behavior cases flaky.
 */
export declare function isolateSchemaEnv(): void;
//# sourceMappingURL=util.d.ts.map