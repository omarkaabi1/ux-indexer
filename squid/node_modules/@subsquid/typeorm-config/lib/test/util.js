"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.SCHEMA_ENV_VARS = exports.DB_ENV_VARS = exports.db_config = void 0;
exports.withClient = withClient;
exports.clearDbEnv = clearDbEnv;
exports.snapshotDbEnv = snapshotDbEnv;
exports.restoreDbEnv = restoreDbEnv;
exports.isolateSchemaEnv = isolateSchemaEnv;
/// <reference types="vitest/globals" />
const pg_1 = require("pg");
exports.db_config = {
    host: process.env.DB_HOST || 'localhost',
    port: parseInt(process.env.DB_PORT || '5432'),
    user: process.env.DB_USER || 'postgres',
    password: process.env.DB_PASS || 'postgres',
    database: process.env.DB_NAME || 'postgres',
};
async function withClient(block) {
    let client = new pg_1.Client(exports.db_config);
    await client.connect();
    try {
        await block(client);
    }
    finally {
        await client.end();
    }
}
/**
 * The DB_* env vars that select a connection. Tests that exercise
 * `createConnectionOptions()` mutate these, so they save/restore around each case.
 */
exports.DB_ENV_VARS = [
    'DB_URL', 'DB_HOST', 'DB_PORT', 'DB_NAME', 'DB_USER', 'DB_PASS',
    'DB_SSL', 'DB_SSL_CA', 'DB_SSL_CA_FILE', 'DB_SSL_CERT', 'DB_SSL_CERT_FILE',
    'DB_SSL_KEY', 'DB_SSL_KEY_FILE', 'DB_SSL_REJECT_UNAUTHORIZED',
    'DB_SCHEMA', 'DB_SCHEMA_INCLUDE_PUBLIC',
];
function clearDbEnv() {
    for (let k of exports.DB_ENV_VARS) {
        delete process.env[k];
    }
}
function snapshotDbEnv() {
    let snap = {};
    for (let k of exports.DB_ENV_VARS) {
        snap[k] = process.env[k];
    }
    return snap;
}
function restoreDbEnv(snap) {
    for (let k of exports.DB_ENV_VARS) {
        let v = snap[k];
        if (v === undefined) {
            delete process.env[k];
        }
        else {
            process.env[k] = v;
        }
    }
}
exports.SCHEMA_ENV_VARS = ['DB_SCHEMA', 'DB_SCHEMA_INCLUDE_PUBLIC'];
/**
 * Registers hooks that clear the schema env vars before each test and restore
 * them after, WITHOUT touching the connection vars (`DB_HOST`/`DB_PORT`/...).
 * Use it in live-DB suites so an ambient `DB_SCHEMA` in the shell/CI can't make
 * the default-behavior cases flaky.
 */
function isolateSchemaEnv() {
    let saved;
    beforeEach(() => {
        saved = {};
        for (let k of exports.SCHEMA_ENV_VARS) {
            saved[k] = process.env[k];
            delete process.env[k];
        }
    });
    afterEach(() => {
        for (let k of exports.SCHEMA_ENV_VARS) {
            let v = saved[k];
            if (v === undefined) {
                delete process.env[k];
            }
            else {
                process.env[k] = v;
            }
        }
    });
}
//# sourceMappingURL=util.js.map