import { type Codec, type Struct, type DecodedStruct, type EncodedStruct } from '@subsquid/evm-codec';
export interface CallRecord {
    input: string;
}
export type FunctionReturn<T extends AbiFunction<any, any>> = T extends AbiFunction<any, infer R> ? R extends Codec<any, infer U> ? U : undefined : never;
export type FunctionArguments<T extends AbiFunction<any, any>> = T extends AbiFunction<infer U, any> ? EncodedStruct<U> : never;
export declare class AbiFunction<const T extends Struct, const R extends Codec<any> | undefined> {
    selector: string;
    readonly returnType?: R | undefined;
    private readonly argsCodec;
    readonly args: T;
    private readonly decodeInline;
    private readonly decodeResultInline?;
    get sighash(): string;
    constructor(selector: string, args: T, returnType?: R | undefined);
    is(calldata: string | CallRecord): boolean;
    encode(args: EncodedStruct<T>): string;
    decode(calldata: string | CallRecord): DecodedStruct<T>;
    decodeResult(output: string): FunctionReturn<typeof this>;
    private createDecodeInline;
    private createDecodeResultInline;
    private checkSignature;
}
export declare const func: <const T extends Struct, const R extends Codec<any> | undefined>(selector: string, args: T, returnType?: R) => AbiFunction<T, R>;
//# sourceMappingURL=function.d.ts.map