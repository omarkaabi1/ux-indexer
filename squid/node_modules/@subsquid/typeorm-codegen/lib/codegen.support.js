"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.modelFromSchema = modelFromSchema;
exports.makeOutDir = makeOutDir;
exports.readGenerated = readGenerated;
exports.listGenerated = listGenerated;
exports.cleanupAll = cleanupAll;
const tools_1 = require("@subsquid/openreader/lib/tools");
const util_internal_code_printer_1 = require("@subsquid/util-internal-code-printer");
const fs_1 = __importDefault(require("fs"));
const os_1 = __importDefault(require("os"));
const path_1 = __importDefault(require("path"));
const tmpDirs = [];
function tmpDir() {
    const dir = fs_1.default.mkdtempSync(path_1.default.join(os_1.default.tmpdir(), 'sqd-codegen-'));
    tmpDirs.push(dir);
    return dir;
}
/**
 * Build a {@link Model} from an in-memory `schema.graphql` string by writing it
 * to a temp file and running openreader's `loadModel` — the same path the CLI
 * uses. openreader's base schema predeclares `@entity`/`@index`/`@unique`/
 * `@fulltext` and all scalars, so the string only needs the `type X @entity {…}`
 * definitions.
 */
function modelFromSchema(schema) {
    const dir = tmpDir();
    const file = path_1.default.join(dir, 'schema.graphql');
    fs_1.default.writeFileSync(file, schema);
    return (0, tools_1.loadModel)(file);
}
/**
 * A fresh {@link OutDir} pointed at a throwaway temp directory. `generateOrmModels`
 * writes real files, so tests read them back from `root`.
 */
function makeOutDir() {
    const root = tmpDir();
    return { dir: new util_internal_code_printer_1.OutDir(root), root };
}
function readGenerated(root, file) {
    return fs_1.default.readFileSync(path_1.default.join(root, file), 'utf8');
}
function listGenerated(root) {
    return fs_1.default.existsSync(root) ? fs_1.default.readdirSync(root) : [];
}
/** Remove every temp directory created during the test run. Call from `afterEach`. */
function cleanupAll() {
    while (tmpDirs.length > 0) {
        const dir = tmpDirs.pop();
        fs_1.default.rmSync(dir, { recursive: true, force: true });
    }
}
//# sourceMappingURL=codegen.support.js.map