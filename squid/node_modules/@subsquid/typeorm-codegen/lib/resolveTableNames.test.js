"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const util_naming_1 = require("@subsquid/util-naming");
const assert_1 = __importDefault(require("assert"));
const vitest_1 = require("vitest");
const codegen_1 = require("./codegen");
// `resolveTableNames` only reads `model[name].kind` and the key, so we can feed
// it bare literals instead of building full openreader models.
function model(...names) {
    const m = {};
    for (const n of names)
        m[n] = { kind: 'entity' };
    return m;
}
// The real over-long entity from PR #514: 69-char snake table name, pinned here.
const LONG_ENTITY = 'LevrConfigProviderSchemaLiquidationBufferBpsOverUnderUpdated';
const LONG_FULL = 'levr_config_provider_schema_liquidation_buffer_bps_over_under_updated';
const LONG_TRUNC = 'levr_config_provider_schema_liquidation_buffer_bps_over_under_u';
(0, vitest_1.describe)('resolveTableNames', () => {
    // Silence (and capture) the truncation warnings the logger writes to stderr.
    let stderr;
    const warned = () => stderr.mock.calls.map((c) => String(c[0])).join('');
    (0, vitest_1.beforeEach)(() => {
        stderr = vitest_1.vi.spyOn(process.stderr, 'write').mockReturnValue(true);
    });
    (0, vitest_1.afterEach)(() => vitest_1.vi.restoreAllMocks());
    (0, vitest_1.it)('maps entity names to snake_case', () => {
        const r = (0, codegen_1.resolveTableNames)(model('MyEntity', 'TokenTransfer'));
        (0, vitest_1.expect)(r.size).toBe(2);
        (0, vitest_1.expect)(r.get('MyEntity')).toBe('my_entity');
        (0, vitest_1.expect)(r.get('TokenTransfer')).toBe('token_transfer');
    });
    (0, vitest_1.it)('excludes non-entity kinds', () => {
        const m = {
            Real: { kind: 'entity' },
            Obj: { kind: 'object' },
            En: { kind: 'enum' },
            Uni: { kind: 'union' },
            Search: { kind: 'fts' },
        };
        const r = (0, codegen_1.resolveTableNames)(m);
        (0, vitest_1.expect)(r.size).toBe(1);
        (0, vitest_1.expect)([...r.keys()]).toEqual(['Real']);
    });
    (0, vitest_1.it)('keeps a 63-char table name unchanged (boundary)', () => {
        const name = 'A' + 'a'.repeat(62); // snake length 63
        assert_1.default.strictEqual((0, util_naming_1.toSnakeCase)(name).length, 63);
        const r = (0, codegen_1.resolveTableNames)(model(name));
        const table = r.get(name);
        (0, vitest_1.expect)(table).toBe((0, util_naming_1.toSnakeCase)(name));
        (0, vitest_1.expect)(table.length).toBe(63);
    });
    (0, vitest_1.it)('truncates a 64-char table name to 63 (boundary)', () => {
        const name = 'A' + 'a'.repeat(63); // snake length 64
        assert_1.default.strictEqual((0, util_naming_1.toSnakeCase)(name).length, 64);
        const table = (0, codegen_1.resolveTableNames)(model(name)).get(name);
        (0, vitest_1.expect)(table.length).toBe(63);
        (0, vitest_1.expect)(table).toBe((0, util_naming_1.toSnakeCase)(name).slice(0, 63));
    });
    (0, vitest_1.it)('truncation is the 63-char prefix of the full name', () => {
        const name = 'A' + 'a'.repeat(63);
        const full = (0, util_naming_1.toSnakeCase)(name);
        const table = (0, codegen_1.resolveTableNames)(model(name)).get(name);
        (0, vitest_1.expect)(full.startsWith(table)).toBe(true);
        (0, vitest_1.expect)(table).toBe(full.slice(0, 63));
    });
    (0, vitest_1.it)('truncates the real-world 69-char name to the exact expected value', () => {
        assert_1.default.strictEqual((0, util_naming_1.toSnakeCase)(LONG_ENTITY), LONG_FULL);
        assert_1.default.strictEqual(LONG_FULL.length, 69);
        const table = (0, codegen_1.resolveTableNames)(model(LONG_ENTITY)).get(LONG_ENTITY);
        (0, vitest_1.expect)(table).toBe(LONG_TRUNC);
        (0, vitest_1.expect)(table.length).toBe(63);
    });
    (0, vitest_1.it)('throws on collision after truncation', () => {
        // Two distinct 64+/65-char names that both truncate to `a`.repeat(63).
        const a = 'A' + 'a'.repeat(63); // snake `a`*64
        const b = 'A' + 'a'.repeat(64); // snake `a`*65
        const fn = () => (0, codegen_1.resolveTableNames)(model(a, b));
        (0, vitest_1.expect)(fn).toThrow(/both map to table name/);
        (0, vitest_1.expect)(fn).toThrow(/after truncation to 63 bytes/);
        (0, vitest_1.expect)(fn).toThrow(new RegExp(`"${a}"`));
        (0, vitest_1.expect)(fn).toThrow(new RegExp(`"${b}"`));
    });
    (0, vitest_1.it)('reports truncation context even when only the prior owner was truncated', () => {
        // Owner truncates to `a`*63; the colliding entity is a *natural* 63-char
        // name (not truncated), processed second. The message must still mention
        // truncation, since the clash is caused by the owner being pruned.
        const truncated = 'A' + 'a'.repeat(63); // snake `a`*64 -> `a`*63
        const natural = 'A' + 'a'.repeat(62); // snake `a`*63, not truncated
        assert_1.default.strictEqual((0, util_naming_1.toSnakeCase)(natural), (0, util_naming_1.toSnakeCase)(truncated).slice(0, 63));
        const fn = () => (0, codegen_1.resolveTableNames)(model(truncated, natural));
        (0, vitest_1.expect)(fn).toThrow(/both map to table name/);
        (0, vitest_1.expect)(fn).toThrow(/after truncation to 63 bytes/);
    });
    (0, vitest_1.it)('throws on collision without truncation', () => {
        // FooBar and Foo_bar both snake to `foo_bar`.
        assert_1.default.strictEqual((0, util_naming_1.toSnakeCase)('FooBar'), (0, util_naming_1.toSnakeCase)('Foo_bar'));
        let err;
        try {
            (0, codegen_1.resolveTableNames)(model('FooBar', 'Foo_bar'));
        }
        catch (e) {
            err = e;
        }
        (0, vitest_1.expect)(err).toBeInstanceOf(Error);
        (0, vitest_1.expect)(err.message).toMatch(/both map to table name "foo_bar"/);
        (0, vitest_1.expect)(err.message).not.toContain('after truncation');
    });
    (0, vitest_1.it)('warns on truncation', () => {
        const overLong = 'A' + 'a'.repeat(63);
        (0, codegen_1.resolveTableNames)(model(overLong));
        (0, vitest_1.expect)(warned()).toContain('exceeds the PostgreSQL identifier');
        (0, vitest_1.expect)(warned()).toContain(overLong);
    });
    (0, vitest_1.it)('stays silent when nothing is truncated', () => {
        (0, codegen_1.resolveTableNames)(model('A' + 'a'.repeat(62))); // 63 chars, not truncated
        (0, vitest_1.expect)(warned()).toBe('');
    });
});
//# sourceMappingURL=resolveTableNames.test.js.map