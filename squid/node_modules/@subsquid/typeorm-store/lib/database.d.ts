import { DataSource } from 'typeorm';
import { TemplateMutation } from './templates';
import { DatabaseState, FinalTxInfo, HashAndHeight, HotTxInfo } from './interfaces';
import { Store } from './store';
export type IsolationLevel = 'SERIALIZABLE' | 'READ COMMITTED' | 'REPEATABLE READ';
export interface TypeormDatabaseOptions {
    supportHotBlocks?: boolean;
    isolationLevel?: IsolationLevel;
    stateSchema?: string;
    projectDir?: string;
}
export interface DatabaseTransactResult {
    templates?: TemplateMutation[];
}
export declare class TypeormDatabase {
    private statusSchema;
    private isolationLevel;
    private con?;
    private projectDir;
    readonly supportsHotBlocks: boolean;
    readonly supportsTemplates: true;
    constructor(options?: TypeormDatabaseOptions);
    connect(): Promise<DatabaseState>;
    disconnect(): Promise<void>;
    protected initializeDataSource(): Promise<DataSource>;
    private initTransaction;
    private getState;
    transact(info: FinalTxInfo, map: (store: Store) => Promise<DatabaseTransactResult | void>): Promise<void>;
    transactHot(info: HotTxInfo, map: (store: Store, block: HashAndHeight) => Promise<void>): Promise<void>;
    transactHot2(info: HotTxInfo, map: (store: Store, sliceBeg: number, sliceEnd: number) => Promise<DatabaseTransactResult | void>): Promise<void>;
    private migrateTemplateRegistryPk;
    private repairOrphans;
    private deleteHotBlocks;
    private insertHotBlock;
    private updateStatus;
    private performUpdates;
    private submit;
    private escapedSchema;
}
//# sourceMappingURL=database.d.ts.map