import { ClientBase } from 'pg';
import { EntityManager } from 'typeorm';
export declare const db_config: {
    host: string;
    port: number;
    user: string;
    password: string;
    database: string;
};
/**
 * Run `block` against a dedicated single connection.
 *
 * Session-scoped `SET`s (work_mem, statement_timeout, ...) are stable here in
 * a way they are not on a pooled EntityManager, where consecutive queries may
 * land on different backends.
 */
export declare function withClient<T>(block: (client: ClientBase) => Promise<T>): Promise<T>;
/**
 * True when the suite is pointed at CockroachDB rather than Postgres.
 *
 * The Makefile runs the whole suite twice against different ports; tests that
 * assert on Postgres planner internals use this to opt out.
 */
export declare function isCockroach(): Promise<boolean>;
export declare function databaseInit(sql: string[]): Promise<void>;
export declare function databaseDelete(): Promise<void>;
export declare function useDatabase(sql: string[]): void;
export declare function getEntityManager(): Promise<EntityManager>;
//# sourceMappingURL=util.d.ts.map