"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.TypeormDatabase = void 0;
const typeorm_config_1 = require("@subsquid/typeorm-config");
const util_internal_1 = require("@subsquid/util-internal");
const assert_1 = __importDefault(require("assert"));
const typeorm_1 = require("typeorm");
const hot_1 = require("./hot");
const templates_1 = require("./templates");
const repair_1 = require("./repair");
const store_1 = require("./store");
class TypeormDatabase {
    constructor(options) {
        this.supportsTemplates = true;
        this.statusSchema = options?.stateSchema || 'squid_processor';
        this.isolationLevel = options?.isolationLevel || 'SERIALIZABLE';
        this.supportsHotBlocks = options?.supportHotBlocks !== false;
        this.projectDir = options?.projectDir || process.cwd();
    }
    async connect() {
        (0, assert_1.default)(this.con == null, 'already connected');
        this.con = await this.initializeDataSource();
        try {
            return await this.con.transaction('SERIALIZABLE', em => this.initTransaction(em));
        }
        catch (e) {
            await this.con.destroy().catch(() => { }); // ignore error
            this.con = undefined;
            throw e;
        }
    }
    async disconnect() {
        await this.con?.destroy().finally(() => this.con = undefined);
    }
    async initializeDataSource() {
        const cfg = (0, typeorm_config_1.createOrmConfig)({ projectDir: this.projectDir });
        const connection = new typeorm_1.DataSource(cfg);
        return connection.initialize();
    }
    async initTransaction(em) {
        let schema = this.escapedSchema();
        await em.query(`CREATE SCHEMA IF NOT EXISTS ${schema}`);
        await em.query(`CREATE TABLE IF NOT EXISTS ${schema}.status (` +
            `id int4 primary key, ` +
            `height int4 not null, ` +
            `hash text DEFAULT '0x', ` +
            `nonce int4 DEFAULT 0` +
            `)`);
        await em.query(
        // for databases created by prev version of typeorm store
        `ALTER TABLE ${schema}.status ADD COLUMN IF NOT EXISTS hash text DEFAULT '0x'`);
        await em.query(
        // for databases created by prev version of typeorm store
        `ALTER TABLE ${schema}.status ADD COLUMN IF NOT EXISTS nonce int DEFAULT 0`);
        await em.query(`CREATE TABLE IF NOT EXISTS ${schema}.hot_block (height int4 primary key, hash text not null)`);
        await em.query(`CREATE TABLE IF NOT EXISTS ${schema}.hot_change_log (` +
            `block_height int4 not null references ${schema}.hot_block on delete cascade, ` +
            `index int4 not null, ` +
            `change jsonb not null, ` +
            `PRIMARY KEY (block_height, index)` +
            `)`);
        await em.query(`CREATE TABLE IF NOT EXISTS ${schema}.template_registry (` +
            `key text not null, ` +
            `value text not null, ` +
            `type boolean not null, ` +
            `block_number int not null, ` +
            `height int not null, ` +
            `PRIMARY KEY (key, value, type, block_number, height)` +
            `)`);
        // Migration: pre-existing tables had PK (key, value, type, block_number)
        // — a hot block re-registering a template at a different height was
        // silently dropped by ON CONFLICT. Promote the PK to include height
        // so re-registrations land at the hot block's height and are visible
        // to state.top[i].templates after reconnect.
        await this.migrateTemplateRegistryPk(em);
        await this.repairOrphans(em);
        let status = await em.query(`SELECT height, hash, nonce FROM ${schema}.status WHERE id = 0`);
        if (status.length == 0) {
            await em.query(`INSERT INTO ${schema}.status (id, height, hash) VALUES (0, -1, '0x')`);
            return assertStateInvariants({ height: -1, hash: '0x', nonce: 0, top: [], templates: [] });
        }
        let rawTemplates = await em.query(`SELECT key, value, type, block_number FROM ${schema}.template_registry ` +
            `WHERE height <= $1 ORDER BY height, block_number, type, key, value`, [status[0].height]);
        let templates = rawTemplates.map(mapTemplateMutation);
        let hotBlocks = await em.query(`SELECT height, hash FROM ${schema}.hot_block ORDER BY height`);
        let top = [];
        for (let block of hotBlocks) {
            let rawBlockTemplates = await em.query(`SELECT key, value, type, block_number FROM ${schema}.template_registry ` +
                `WHERE height = $1 ORDER BY block_number, type, key, value`, [block.height]);
            top.push({ ...block, templates: rawBlockTemplates.map(mapTemplateMutation) });
        }
        return assertStateInvariants({ ...status[0], top, templates });
    }
    async getState(em) {
        let schema = this.escapedSchema();
        let status = await em.query(`SELECT height, hash, nonce FROM ${schema}.status WHERE id = 0`);
        (0, assert_1.default)(status.length == 1);
        let rawTop = await em.query(`SELECT hash, height FROM ${schema}.hot_block ORDER BY height`);
        let top = rawTop.map(b => ({ ...b, templates: [] }));
        return assertStateInvariants({ ...status[0], top, templates: [] });
    }
    transact(info, map) {
        return this.submit(async (em) => {
            let state = await this.getState(em);
            let { prevHead: prev, nextHead: next } = info;
            (0, assert_1.default)(state.hash === info.prevHead.hash, RACE_MSG);
            (0, assert_1.default)(state.height === prev.height);
            (0, assert_1.default)(prev.height < next.height);
            (0, assert_1.default)(prev.hash != next.hash);
            for (let i = state.top.length - 1; i >= 0; i--) {
                let block = state.top[i];
                await (0, hot_1.rollbackBlock)(this.statusSchema, em, block.height);
            }
            await this.performUpdates(map, em, new templates_1.TemplateRegistryTracker(em, this.statusSchema, next.height));
            await this.updateStatus(em, state.nonce, next);
        });
    }
    transactHot(info, map) {
        return this.transactHot2(info, async (store, sliceBeg, sliceEnd) => {
            for (let i = sliceBeg; i < sliceEnd; i++) {
                await map(store, info.newBlocks[i]);
            }
        });
    }
    transactHot2(info, map) {
        return this.submit(async (em) => {
            let state = await this.getState(em);
            let chain = [state, ...state.top];
            assertChainContinuity(info.baseHead, info.newBlocks);
            (0, assert_1.default)(info.finalizedHead.height <= ((0, util_internal_1.maybeLast)(info.newBlocks) ?? info.baseHead).height);
            let baseHeadPos = chain.findIndex(b => b.hash === info.baseHead.hash);
            (0, assert_1.default)(baseHeadPos >= 0, RACE_MSG);
            // Stale finalizedHead from upstream is normal during gateway→RPC
            // handoff. Clamp to our committed finalized state so we never
            // regress status, but still process the new hot blocks.
            let effectiveFinalizedHead = info.finalizedHead.height >= chain[0].height ? info.finalizedHead : chain[0];
            let rollbackPos = baseHeadPos + 1;
            for (let i = chain.length - 1; i >= rollbackPos; i--) {
                await (0, hot_1.rollbackBlock)(this.statusSchema, em, chain[i].height);
            }
            if (info.newBlocks.length) {
                let unfinalizedStart = info.newBlocks.findIndex(b => b.height > effectiveFinalizedHead.height);
                if (unfinalizedStart < 0) {
                    unfinalizedStart = info.newBlocks.length;
                }
                if (unfinalizedStart > 0) {
                    await this.performUpdates(async (store) => map(store, 0, unfinalizedStart), em, new templates_1.TemplateRegistryTracker(em, this.statusSchema, effectiveFinalizedHead.height));
                }
                if (unfinalizedStart < info.newBlocks.length) {
                    // To prevent transaction timeouts when handling many unfinalized blocks,
                    // we group the user callback instead of handling each block individually.
                    // Every block in the slice still gets its own hot_block row so any
                    // height in the chain is addressable as a future baseHead.
                    let groupSize = Math.max(1, Math.floor((info.newBlocks.length - unfinalizedStart) / 100));
                    for (let i = unfinalizedStart; i < info.newBlocks.length; i += groupSize) {
                        let sliceEnd = Math.min(i + groupSize, info.newBlocks.length);
                        let lastBlock = info.newBlocks[sliceEnd - 1];
                        await this.insertHotBlock(em, lastBlock);
                        await this.performUpdates(async (store) => map(store, i, sliceEnd), em, new templates_1.TemplateRegistryTracker(em, this.statusSchema, lastBlock.height), new hot_1.ChangeTracker(em, this.statusSchema, lastBlock.height));
                    }
                }
            }
            chain = chain.slice(0, rollbackPos).concat(info.newBlocks);
            await this.deleteHotBlocks(em, effectiveFinalizedHead.height);
            await this.updateStatus(em, state.nonce, effectiveFinalizedHead);
        });
    }
    async migrateTemplateRegistryPk(em) {
        let schema = this.escapedSchema();
        let pkCols = await em.query(`SELECT a.attname
               FROM pg_index i
               JOIN pg_attribute a ON a.attrelid = i.indrelid AND a.attnum = ANY(i.indkey)
               JOIN pg_class c ON c.oid = i.indrelid
               JOIN pg_namespace n ON n.oid = c.relnamespace
              WHERE c.relname = 'template_registry' AND n.nspname = $1 AND i.indisprimary`, [this.statusSchema]);
        if (pkCols.length === 0 || pkCols.some(r => r.attname === 'height'))
            return;
        let cons = await em.query(`SELECT conname FROM pg_constraint
              WHERE contype = 'p' AND conrelid = '${this.statusSchema}.template_registry'::regclass`);
        if (cons.length) {
            await em.query(`ALTER TABLE ${schema}.template_registry DROP CONSTRAINT "${cons[0].conname}"`);
        }
        await em.query(`ALTER TABLE ${schema}.template_registry ADD PRIMARY KEY (key, value, type, block_number, height)`);
    }
    async repairOrphans(em) {
        // Written as NOT EXISTS anti-joins rather than NOT IN (SELECT ...) —
        // see the doc comment on orphanRepairStatements for why that matters.
        for (let sql of (0, repair_1.orphanRepairStatements)(this.escapedSchema())) {
            await em.query(sql);
        }
    }
    deleteHotBlocks(em, finalizedHeight) {
        return em.query(`DELETE FROM ${this.escapedSchema()}.hot_block WHERE height <= $1`, [finalizedHeight]);
    }
    async insertHotBlock(em, block) {
        let schema = this.escapedSchema();
        await em.query(`INSERT INTO ${schema}.hot_block (height, hash) VALUES ($1, $2)`, [block.height, block.hash]);
        // Sentinel marker so connect() can distinguish a hot_block row that
        // was inserted via the official path (always paired with this row)
        // from one that was inserted out-of-band (manual SQL, partial restore).
        // The marker has an unrecognized `kind`; rollbackBlock's switch falls
        // through and ignores it.
        await em.query(`INSERT INTO ${schema}.hot_change_log (block_height, index, change) VALUES ($1, 0, $2::jsonb)`, [block.height, JSON.stringify({ kind: 'sentinel' })]);
    }
    async updateStatus(em, nonce, next) {
        let schema = this.escapedSchema();
        let result = await em.query(`UPDATE ${schema}.status SET height = $1, hash = $2, nonce = nonce + 1 WHERE id = 0 AND nonce = $3`, [next.height, next.hash, nonce]);
        let rowsChanged = result[1];
        // Will never happen if isolation level is SERIALIZABLE or REPEATABLE_READ,
        // but occasionally people use multiprocessor setups and READ_COMMITTED.
        assert_1.default.strictEqual(rowsChanged, 1, RACE_MSG);
    }
    async performUpdates(cb, em, templateRegistry, changeTracker) {
        let running = true;
        let store = new store_1.Store(() => {
            (0, assert_1.default)(running, `too late to perform db updates, make sure you haven't forgot to await on db query`);
            return em;
        }, changeTracker);
        try {
            let result = await cb(store);
            if (result?.templates) {
                await templateRegistry.persist(result.templates);
            }
        }
        finally {
            running = false;
        }
    }
    async submit(tx) {
        let retries = 3;
        while (true) {
            try {
                let con = this.con;
                (0, assert_1.default)(con != null, 'not connected');
                return await con.transaction(this.isolationLevel, tx);
            }
            catch (e) {
                if (e.code == '40001' && retries) {
                    retries -= 1;
                }
                else {
                    throw e;
                }
            }
        }
    }
    escapedSchema() {
        let con = (0, util_internal_1.assertNotNull)(this.con);
        return con.driver.escape(this.statusSchema);
    }
}
exports.TypeormDatabase = TypeormDatabase;
const RACE_MSG = 'status table was updated by foreign process, make sure no other processor is running';
function mapTemplateMutation(row) {
    return {
        type: row.type ? 'add' : 'delete',
        key: row.key,
        value: row.value,
        blockNumber: Number(row.block_number),
    };
}
function assertStateInvariants(state) {
    let height = state.height;
    // Sanity check. Who knows what driver will return?
    (0, assert_1.default)(Number.isSafeInteger(height));
    assertChainContinuity(state, state.top);
    return state;
}
function assertChainContinuity(base, chain) {
    let prev = base;
    for (let b of chain) {
        (0, assert_1.default)(b.height > prev.height, 'blocks must form a continues chain');
        prev = b;
    }
}
//# sourceMappingURL=database.js.map