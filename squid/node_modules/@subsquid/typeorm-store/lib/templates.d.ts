import type { EntityManager } from 'typeorm';
export interface TemplateMutation {
    type: 'add' | 'delete';
    key: string;
    value: string;
    blockNumber: number;
}
export declare class TemplateRegistryTracker {
    private readonly em;
    private readonly statusSchema;
    private readonly height;
    constructor(em: EntityManager, statusSchema: string, height: number);
    persist(mutations: TemplateMutation[]): Promise<void>;
}
//# sourceMappingURL=templates.d.ts.map